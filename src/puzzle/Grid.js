import PuzzleTemplate from './PuzzleTemplate.svg'


const Split = () => {
    let width = 800
    let height = 600
    let cols = 8;
    let rows = 8;
    let pointsPerPiece = 100;
    let pieceHeight = 2;

    let medianWidth = width/cols;
    let medianHeight = height/rows;

    let template = [PuzzleTemplate]
    return
}

export default Split